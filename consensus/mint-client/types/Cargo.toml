[package]
name = "mc-consensus-mint-client-types"
version = "2.1.0-pre0"
authors = ["MobileCoin"]
edition = "2021"

[lib]
path = "src/lib.rs"

[dependencies]
mc-consensus-service-config = { path = "../../service/config" }
mc-crypto-keys = { path = "../../../crypto/keys" }
mc-transaction-core = { path = "../../../transaction/core" }

displaydoc = "0.2"
hex = { version = "0.4", features = ["serde"] }
serde = "1"
serde_json = "1.0"
