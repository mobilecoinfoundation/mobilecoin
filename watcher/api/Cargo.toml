[package]
name = "mc-watcher-api"
version = "0.5.0"
authors = ["MobileCoin"]
edition = "2018"

[features]
default = []
std = ["mc-util-serial/std"]

[dependencies]
displaydoc = {version = "0.1", default-features = false }
serde = { version = "1.0", default-features = false, features = ["alloc", "derive"] }

mc-util-serial = { path = "../../util/serial", default-features = false }
