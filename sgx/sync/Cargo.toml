[package]
name = "mc-sgx-sync"
version = "4.0.2"
authors = ["MobileCoin"]
license = "GPL-3.0"
readme = "README.md"

[dependencies]
mc-sgx-types = { path = "../types" }
mc-sgx-panic = { path = "../panic", optional = true }

[features]
default = ["mc-sgx-panic"]
