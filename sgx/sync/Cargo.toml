[package]
name = "mc-sgx-sync"
version = "1.1.2"
authors = ["MobileCoin"]

[dependencies]
mc-sgx-types = { path = "../types" }
mc-sgx-panic = { path = "../panic", optional = true }

[features]
default = ["mc-sgx-panic"]
