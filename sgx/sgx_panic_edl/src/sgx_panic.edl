// Copyright (c) 2018-2020 MobileCoin Inc.

enclave {
    untrusted {
        // sgx_panic
        void report_panic_message([in, size=msg_len] const uint8_t* msg,
                                  size_t msg_len);
    };
};
