[package]
name = "mc-sgx-backtrace"
version = "0.1.5-alpha-1"
authors = ["MobileCoin"]
edition = "2018"

[features]
default = ["mc-sgx-debug"]

[dependencies]
mc-sgx-enclave-id = { path = "../enclave_id" }
mc-sgx-libc-types = { path = "../libc-types" }
mc-sgx-types = { path = "../types" }
mc-sgx-unwind = { path = "../unwind" }
mc-sgx-debug = { path = "../debug", optional = true }
