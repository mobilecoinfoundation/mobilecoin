[package]
name = "mc-sgx-service"
version = "2.1.0-pre0"
authors = ["MobileCoin"]
edition = "2021"

[dependencies]
mc-sgx-types = { path = "../types" }

[build-dependencies]
mc-sgx-build = { path = "../build" }
