[package]
name = "mc-sgx-service"
version = "4.0.1"
authors = ["MobileCoin"]
edition = "2021"

[dependencies]
mc-sgx-types = { path = "../types" }

[build-dependencies]
mc-sgx-build = { path = "../build" }
