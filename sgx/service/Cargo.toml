[package]
name = "mc-sgx-service"
version = "4.1.0"
authors = ["MobileCoin"]
edition = "2021"
license = "GPL-3.0"
readme = "README.md"

[dependencies]
mc-sgx-types = { path = "../types" }

[build-dependencies]
mc-sgx-build = { path = "../build" }
