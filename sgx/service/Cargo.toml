[package]
name = "mc-sgx-service"
version = "0.6.0"
authors = ["MobileCoin"]
edition = "2018"

[dependencies]
mc-sgx-types = { path = "../types" }

[build-dependencies]
mc-sgx-build = { path = "../build" }
