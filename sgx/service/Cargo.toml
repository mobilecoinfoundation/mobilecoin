[package]
name = "mc-sgx-service"
version = "1.1.2"
authors = ["MobileCoin"]
edition = "2018"

[dependencies]
mc-sgx-types = { path = "../types" }

[build-dependencies]
mc-sgx-build = { path = "../build" }
