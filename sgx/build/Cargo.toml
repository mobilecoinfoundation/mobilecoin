[package]
name = "mc-sgx-build"
version = "4.0.2"
authors = ["MobileCoin"]
edition = "2021"
license = "GPL-3.0"
readme = "README.md"

[dependencies]
cc = "1.0"
lazy_static = "1.4"
serde = { version = "1", default-features = false, features = ["alloc", "derive"] }
walkdir = "2"
