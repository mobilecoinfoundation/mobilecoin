[package]
name = "mc-sgx-urts"
version = "7.1.0"
authors = ["MobileCoin"]
license = "GPL-3.0"
readme = "README.md"
# TODO: Consider making sgx a self-contained workspace and get the value from there.
rust-version = "1.83.0"
edition = "2021"

[dependencies]
mc-common = { workspace = true, features = ["log"] }
mc-sgx-slog = { workspace = true }
mc-sgx-types = { workspace = true }

prost = { workspace = true, features = ["prost-derive"] }

[build-dependencies]
mc-sgx-build = { workspace = true }
