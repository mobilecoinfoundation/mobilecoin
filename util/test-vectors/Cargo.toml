[package]
name = "mc-util-test-vectors"
version = "0.5.0"
authors = ["MobileCoin"]
edition = "2018"

[dependencies]
datatest = "0.6"
serde = "1.0"
serde_json = "1.0"

# Even though serde_cbor isn't used, serde_cbor/std must be enabled whenever serde/std is
serde_cbor = { version = "0.11", features = ["std"] }
