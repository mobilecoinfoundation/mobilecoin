[package]
name = "mc-util-parse"
version = "2.0.0"
authors = ["MobileCoin"]
edition = "2021"
description = "Helpers for parsing, particularly, for use with Clap and similar"
readme = "README.md"

[dependencies]
itertools = "0.10"
mc-sgx-css = { path = "../../sgx/css" }
