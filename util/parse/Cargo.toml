[package]
name = "mc-util-parse"
version = "1.2.2"
authors = ["MobileCoin"]
edition = "2018"
description = "Helpers for parsing, particularly, for use with Clap and similar"
readme = "README.md"

[dependencies]
itertools = "0.10"
mc-sgx-css = { path = "../../sgx/css" }
