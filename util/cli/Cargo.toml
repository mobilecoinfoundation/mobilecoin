[package]
name = "mc-util-cli"
version = "2.0.0"
authors = ["MobileCoin"]
edition = "2021"

[dependencies]
mc-util-build-info = { path = "../build/info" }

clap = { version = "3.2" }

[dev-dependencies]
clap = { version = "3.2", features = ['derive'] }
