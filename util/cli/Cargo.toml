[package]
name = "mc-util-cli"
version = "2.1.0-pre0"
authors = ["MobileCoin"]
edition = "2021"

[dependencies]
mc-util-build-info = { path = "../build/info" }

clap = { version = "3.2" }

[dev-dependencies]
clap = { version = "3.2", features = ['derive'] }
