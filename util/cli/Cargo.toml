[package]
name = "mc-util-cli"
version = "4.0.0"
authors = ["MobileCoin"]
edition = "2021"

[dependencies]
mc-util-build-info = { path = "../build/info" }

clap = { version = "4.0", features = ["string"] }

[dev-dependencies]
clap = { version = "4.0", features = ["derive"] }
