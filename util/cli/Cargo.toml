[package]
name = "mc-util-cli"
version = "2.0.0"
authors = ["MobileCoin"]
edition = "2018"

[dependencies]
mc-util-build-info = { path = "../build/info" }

clap = { version = "3.1" }

[dev-dependencies]
clap = { version = "3.1", features = ['derive'] }
